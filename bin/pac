#!/usr/bin/env bash

run(){
    echo pacman "$@"
    sudo pacman "$@"
}


usage(){
    cat <<__EOF__
Simple Pacman Wrapper
usage: pac COMMAND [OPTIONS] [TARGET]
    COMMANDS:
        install
        update
        list
	info
        help
    OPTIONS
    TARGET
        see pacman(8)
__EOF__

    [ "$1" == "help" ] && exit 0 || exit 1
}


command=$1
shift
case $command in
    install)
        run -S "$@"
        ;;
    update)
        run -Syu "$@"
        ;;
    info)
        run -Si "$@"
        ;;
    list)
        run -Q "$@"
        ;;
    *)
        usage
        ;;
esac
